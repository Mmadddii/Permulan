% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05filtraketa.R
\name{filter_metrics}
\alias{filter_metrics}
\alias{filter_metrics,data.frame-method}
\alias{filter_metrics,DatuMultzoa-method}
\title{Atributuen filtraketa metriketan oinarrituta}
\usage{
filter_metrics(x, ...)

\S4method{filter_metrics}{data.frame}(x, var_min = 0, entropy_min = 0, auc_min = 0, klase = NULL, ...)

\S4method{filter_metrics}{DatuMultzoa}(x, var_min = 0, entropy_min = 0, auc_min = 0, ...)
}
\arguments{
\item{x}{Dataset bat (`data.frame` edo `DatuMultzoa`)}

\item{...}{Argumentu gehigarriak; metodoen arteko bateragarritasunerako.}

\item{var_min}{Bariantza minimoa (lehenetsia = 0)}

\item{entropy_min}{Entropia minimoa (lehenetsia = 0)}

\item{auc_min}{AUC minimoa (lehenetsia = 0)}

\item{klase}{Klase aldagaia edo bektore binarioa AUC kalkulatzeko (`data.frame` metodoan bakarrik erabiltzen da)}
}
\value{
Filtratutako objektu berria (`data.frame` edo `DatuMultzoa`)
}
\description{
Funtzio hauek dataset baten atributuak filtratzen dituzte
aurrez kalkulatutako metrikak kontuan hartuta:
\itemize{
  \item Bariantza minimoa atributu numerikoentzat
  \item Entropia minimoa atributu kategorikoentzat
  \item AUC minimoa atributu numerikoentzat (klasea emanda)
}

Metodoak `data.frame` eta `DatuMultzoa` klaseetarako definituta daude.
}
\examples{
df <- data.frame(
  x1 = c(1,2,3,4,5),
  x2 = c(5,5,5,5,5),
  cat = factor(c("A","B","A","B","C")),
  label = c(TRUE, FALSE, TRUE, FALSE, TRUE)
)

# Data.frame baten adibidea
filter_metrics(df, var_min = 1, entropy_min = 0.5)

# DatuMultzoa objektu baten adibidea
dm <- DatuMultzoa(df, klase_izena = "label")
filter_metrics(dm, var_min = 1, entropy_min = 0.5)

}
\seealso{
`var_col()` — atributu numerikoen bariantza kalkulatzeko,
`entropy()` — atributu kategorikoen entropia kalkulatzeko,
`roc_analisi()` — AUC kalkulatzeko.
}
